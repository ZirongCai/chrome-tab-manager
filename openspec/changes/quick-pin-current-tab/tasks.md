## 1. Core Logic

- [x] 1.1 Add helper function to check if a URL is user-pinned (use existing `isUserPinned` or implement locally)
- [x] 1.2 Add `togglePinCurrentTab()` function to pin/unpin the active tab

## 2. UI Integration

- [x] 2.1 Modify `renderBrowseTab()` to add pin button only for active tab
- [x] 2.2 Add click handler for the pin button in `setupBrowseClickHandlers()`

## 3. User Feedback

- [x] 3.1 Show toast notification after pin/unpin action
- [x] 3.2 Update pin button state after toggle (visual feedback)

## 4. Styling

- [x] 4.1 Add CSS styles for the browse mode pin button (consistent with existing pin button in Search Mode)
